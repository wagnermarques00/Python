# Using the len() function to get the number of elements in a set.

# Creating a set
numbers = {1, 2, 3, 1, 2, 4, 5, 5, 6, 7, 8, 9, 0}

# Getting the number of elements in the set.
numbers_length = len(numbers)
print(numbers_length)  # 10
